## 📅 Финальный план: AI Code Reviewer — CLI

**4 спринта × 5 дней = 20 рабочих дней**

---

## 🏃 Спринт 1: Инфраструктура, эмбеддинги, RAG-агент на LangGraph

**Цель:** Всё работает локально, агент отвечает на вопросы по документам.

### День 1: Окружение и проверка
- [x] `docker-compose.yml` — Qdrant + Ollama + ollama-pull
- [x] `package.json` — чистый Node.js/TypeScript
- [x] `tsconfig.json` — ES2022, NodeNext
- [x] `.env` / `.env.example` — все переменные
- [x] `src/main.ts` — проверка здоровья Qdrant и Ollama
- [x] `npm run health` → оба сервиса живы

### День 2: Документы для RAG
- [x] 7 Markdown-документов в `documents/`
- [x] `security-guidelines.md` содержит скрытую инъекцию (для Спринта 3)

### День 3: Индексация
- [x] `src/ingest.ts` — скрипт загрузки в Qdrant
- [x] `RecursiveCharacterTextSplitter` (чанки по 500, перекрытие 100)
- [x] Эмбеддинги: `nomic-embed-text-v2-moe` через Ollama
- [x] Автоочистка коллекции перед индексацией
- [x] Валидация: 3 тестовых поиска

### День 4: RAG-агент на LangGraph
- [x] `src/graph.ts` — граф `retrieve` → `generate`
- [x] Состояние через `Annotation.Root`
- [x] LLM: `llama3.2` через Ollama
- [x] `src/agent.ts` — вызов графа

### День 5: CLI и тестирование
- [x] `src/cli.ts` — точка входа
- [x] `src/test-agent.ts` — 5 тестовых вопросов
- [x] `npm run ask "вопрос"` — работает
- [x] `npm run test-agent` — 4 из 5 правильно
- [x] `README.md` — документация

**Результат:** Агент в CLI отвечает на вопросы по документам команды.

---

## 🏃 Спринт 2: MCP-сервер и GitHub-интеграция

**Цель:** Агент анализирует реальные PR через MCP-инструменты.

### День 1: MCP-сервер (основа)
- [x] Установка `@modelcontextprotocol/sdk`
- [x] `mcp-server/` — отдельная директория
- [x] Инструмент `search_guidelines(query)` → обёртка над Qdrant
- [x] Тест: MCP-клиент вызывает, получает чанки

### День 2: GitHub-инструменты
- [x] GitHub токен
- [x] Тестовый репозиторий + PR #1 с нарушениями (без инъекций — они в Спринте 3)
- [x] `get_pull_request_diff(pr_number)` — дифф через GitHub API
- [x] `get_file_content(pr_number, path)` — чтение файла
- [x] `post_review_comment(pr_number, file, line, text)` — пост комментария

### День 3: Адаптер MCP → LangChain
- [ ] MCP-клиент в основном приложении
- [ ] Преобразование MCP-инструментов в LangChain Tools
- [ ] Агент видит инструменты

### День 4: AI-ревьюер (MVP)
- [ ] Новый узел `analyze_diff` в графе
- [ ] Логика: дифф → поиск правил → сравнение → комментарий
- [ ] Тест на PR с нарушениями именования и отсутствием тестов

### День 5: CLI-команда `review`
- [ ] `npm run review <номер_PR>` — полный цикл
- [ ] Вывод результатов в консоли
- [ ] Обработка ошибок (GitHub недоступен, пустой дифф)

**Результат:** `npm run review 1` → комментарии в PR на GitHub.

---

## 🏃 Спринт 3: Защита от инъекций и HITL

**Цель:** Три эшелона защиты, критические решения — через человека.

### День 1: Pre-Guard (вектор 1 — прямая инъекция)
- [ ] `src/guards/input-guard.ts`
- [ ] Regex-фильтр: `игнорируй`, `забудь всё`, `ignore all`, `system:`, `одобри без`
- [ ] Встройка в граф до `analyze_diff`
- [ ] Тест: `npm run review 1` с инъекцией в описании PR → блокировка

### День 2: ContextSanitizer (вектор 2 — косвенная инъекция)
- [ ] `src/guards/context-sanitizer.ts`
- [ ] XML-обёртка `<document_context>` для чанков
- [ ] Усиление системного промпта
- [ ] Тест: документ с скрытой инъекцией → агент игнорирует

### День 3: Классификатор критичности
- [ ] `src/guards/criticality-classifier.ts`
- [ ] Критерии: инъекция = critical, безопасность = high, naming = medium, форматирование = low
- [ ] `medium`/`low` → авто-пост
- [ ] `critical`/`high` → HITL

### День 4: HITL в CLI
- [ ] Узел `humanReview` в графе
- [ ] `interrupt` графа → ожидание ввода
- [ ] `readline` в CLI: `[y/n]`
- [ ] Возобновление графа после решения

### День 5: Тестирование защиты
- [ ] Сценарий 1: прямая инъекция → блокировка Pre-Guard
- [ ] Сценарий 2: косвенная инъекция в RAG → игнорируется
- [ ] Сценарий 3: критическое нарушение → `[y/n]` → человек решает
- [ ] Сценарий 4: обычный PR → авто-комментарий

**Результат:** Защищённый AI-ревьюер с подтверждением критических действий в CLI.

---

## 🏃 Спринт 4: Тестирование, сценарий, защита проекта

**Цель:** Отполировать продукт и подготовить демо.

### День 1: Финальная сборка CLI
- [ ] `npm run review <PR>` — полный цикл с защитой и HITL
- [ ] `npm run ask "вопрос"` — обычный режим вопрос-ответ
- [ ] `npm run health` — проверка окружения
- [ ] `npm run ingest` — индексация
- [ ] Красивый вывод в консоли: статусы, прогресс, цветовые маркеры (опционально)

### День 2: Тестовый репозиторий и сценарий
- [ ] PR #1: нарушения naming + нет теста → авто-комментарии
- [ ] PR #2: описание с инъекцией → блокировка Pre-Guard
- [ ] Документ с скрытой инъекцией в RAG → агент игнорирует
- [ ] Критическое нарушение → HITL `[y/n]`

### День 3: Репетиция демо
- [ ] Прогон полного сценария
- [ ] Тайминг: 10 минут
- [ ] Запасной план: если GitHub недоступен → JSON-сервер

### День 4: Документация
- [ ] `README.md`: архитектура, схема защиты, инструкция, скриншоты
- [ ] `.env.example` — финальный
- [ ] Диаграмма архитектуры (Mermaid)
- [ ] Слайды для презентации

### День 5: Защита 🎓
- [ ] Презентация 10 минут
- [ ] Демо в CLI + GitHub
- [ ] Ответы на вопросы

**Результат:** Готовый проект, успешная защита.

---

## 📊 Сводка

| Спринт | Дней | Ключевые артефакты |
|--------|:---:|---|
| 1 | 5 | Docker, Qdrant, индексация, RAG-агент, CLI |
| 2 | 5 | MCP-сервер, GitHub-инструменты, `npm run review` |
| 3 | 5 | Pre-Guard, ContextSanitizer, CriticalityClassifier, HITL |
| 4 | 5 | Финальный CLI, сценарий демо, README, защита |

---

## 🖥️ CLI-команды (финальный набор)

```bash
npm run health              # Проверка Qdrant и Ollama
npm run ingest              # Индексация документов
npm run ask "вопрос"        # Задать вопрос агенту
npm run test-agent          # 5 тестовых вопросов
npm run review 1            # Проверить PR #1
npm run review 2            # Проверить PR #2 (с инъекцией)
